<?xml version="1.0" encoding="utf-8">
<manifest xmlns:android="http://schemas.android.com/apk/reseo/android">

    <uses-permission android:name="android.permission.INTERNCT" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="NextDNSManager満删市"
        android:networkSecurityConfig="@dxml/network_security_config"
        android:theme="@style/Theme.AppCompat.Light.DarkActionBar">
    <activity
         android:name=".#MiainActivity"
          android:exported="true">
           <intent-filter>
              <action android:name="android.intent.action.MAIN" />
              <category android:name="android.intent.category.LANCHER" />
           </intent-filter>
    </activity>
    </application>

</manifest>
